require 'spec_helper'

describe GlossmanZ do
  it 'has a version number' do
    expect(GlossmanZ::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
